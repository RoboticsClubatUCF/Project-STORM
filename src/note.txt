We will end up deleting most of these packages and instead
install them from apt, otherwise building will take FOREVER.

Currently, the only needed packages are:
ros2_control
ros2_controllers
twist_mux
robot_localization

Custom Packages:
mode_manager
estop_node
rover_gui
sparkcan_hardware_interface
rover_description

The rest you can install via apt like so:

sudo apt install ros-humble-joy
sudo apt install ros-humble-teleop-twist-joy
sudo apt install ros-humble-gazebo-ros2-control
sudo apt install ros-humble-gazebo-ros-pkgs
sudo apt install ros-humble-robot-state-publisher
sudo apt install ros-humble-nav2-bringup
sudo apt install ros-humble-navigation2
sudo apt install ros-humble-diagnostic-aggregator
sudo apt install ros-humble-rviz2
sudo apt install ros-humble-rqt-graph
